export interface Package {
  id: string;
  name: string;
  image: string;
  dataAiHint: string;
  duration: string;
}

export interface Testimonial {
  id: string;
  quote: string;
  author: string;
  location: string;
}
